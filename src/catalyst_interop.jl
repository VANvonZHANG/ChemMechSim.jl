# Catalyst interoperability. The ReactionSystem type annotation is added when
# Catalyst becomes a dependency (MVP/Phase 1). (spec §3.3, §4)

"Import a Catalyst ReactionSystem (mass-action subset) as a Mechanism. (stub — Phase 1)"
import_from_catalyst(rn; kwargs...) =
    error("import_from_catalyst: not implemented in framework scaffold; see the MVP (Phase 1) plan.")
